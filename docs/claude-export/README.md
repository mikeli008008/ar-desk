# Claude export PDFs

Source PDFs Han attached when bootstrapping this repo (README / main / config / engine).
Canonical source of truth is the `.py` / `.yaml` / `README.md` at the repo root — these PDFs are archival.
